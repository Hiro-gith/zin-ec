<!--ログイン画面-->
<% provide(:title, "Cart_Log in") %>


<div class="row justify-content-center">
  <div class="col-md-6 col-md-offset-3">
    <div class="login-form">
      <p class="kaiinn-login">会員ログイン</p>
      <%= form_for(:session, url: cart_login_create_path) do |f| %>
  
        <p class="login-mail">メールアドレス</p>
        <%= f.email_field :email, class: 'form-control' %>
        <p class="login-admin">管理者：admin@arazin.org</p>
        <p>一般ユーザー：general@arazin.org</p>
  
        <p class="login-password">パスワード</p>
        <%= f.password_field :password, class: 'form-control' %>
        <p class="login-admin">管理者：1arazinzinA$</p>
        <p>一般ユーザー：2arazinzinG$</p>
        
        <%= f.label :remember_me, class: "checkbox inline" do %>
          <%= f.check_box :remember_me %>
          <span>Remember me on this computer</span>
        <% end %>
  
        <%= f.submit "ログイン", class: "btn btn-primary" %>
      <% end %>
  
      <p>まだ会員登録されていない方　 <%= link_to "新規登録（無料）", signup_path %></p>
    </div>
  </div>
</div>