<div class="pagination"><%= paginate @items %></div>
<% @items.each do |item| %>
<li id="item-<%= item.id %>">
  <span class="item_name"><%= link_to item.name, item.name %></span>
  <span class="item_price"><%=item.price %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(item.created_at) %> ago.
  </span>
  <% end %>
</li>
