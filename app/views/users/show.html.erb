<!--プロフィール（会員情報）-->
<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
        
        
        <% if current_user?(@user) %>
          <div>
            <%= link_to "ユーザー設定の変更", edit_user_path(current_user) %>
          </div>
          <div>
            <%= link_to "商品出品登録",new_item_path %>
          </div>
          
        <% end %>

      </h1>
    </section>
  </aside>
  <div class="col-md-8">
    <div class="user-put-up">
      <!--アイテムがあれば-->
      <% if @user.items.any?%>
        <!--アイテム数-->
        <h3>出品している商品 (<%= @user.items.count %>)</h3>
        <ol class="items">
          <%= render "items/items" %>
        </ol>
      <% end %>
    </div>
  </div>
</div>